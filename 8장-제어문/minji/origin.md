<aside>
✅

제어문을 사용하면 코드의 실행 흐름을 인위적으로 변경할 수 있음 → 직관적인 코드의 흐름을 혼란스럽게 만들어서 가독성을 해침 → 고차 함수를 사용한 프로그래밍 기법을 통해 제어문 사용을 억제

</aside>

## 블록문

- 블록문: 0개 이상의 문을 중괄호로 묶은 것. 코드 블록 또는 블록이라고도 부름.
- 하나의 실행 단위로 취급
- 단독으로 사용할 수도 있으나 일반적으로 제어문이나 함수를 정의할 때 사용하는 것이 일반적
- 끝에 세미콜론;을 붙이지 않음

```jsx
// 일반 블록문
{
  var foo = 10;
}

// 제어문
var x = 1;
if (x < 10) {
  x++;
}

// 함수 선언문
function sum(a, b) {
  return a + b;
}
```

## 조건문

- 조건문: 주어진 조건식(불리언 값으로 평가될 수 있는 표현식)의 평가 결과에 따라 코드 블록(블록문)의 실행을 결정
- if - else 와 switch 문이 있음

### if- else문

- else if, else 은 옵션
- 코드 블록 내의 문이 하나 뿐이라면 중괄호 생략 가능
  ```jsx
  var num = 2;
  var kind;

  if (num > 0) kind = "양수"; // 중괄호 생략
  ```

### switch문

- 주어진 표현식을 평가하여 그 값과 일치하는 표현식을 갖는 case 문으로 실행 흐름을 옮김
  ```jsx
  switch (표현식){
    case 표현식1:
      switch 문의 표현식과 표현식1이 일치하면 실행될 문;
      break;
    case 표현식2:
      switch 문의 표현식과 표현식2이 일치하면 실행될 문;
      break;
    default:
      switch 문의 표현식과 일치하는 case문이 없을 때 실행될 문;
  }
  ```
- switch 문의 표현식과 일치하는 case문이 없다면 실행 순서는 default 문으로 이동. default 는 옵션
- switch 문의 표현식은 불리언 값보다는 문자열이나 숫자를 많이 사용함
- break 문을 사용해서 탈출하지 않으면 switch 문의 모든 case 문을 실행하는 폴스루(fall through)가 나타날 수 있음
- default 문에는 break 를 생략하는 것이 일반적

## 반복문

- 반복문: 조건식의 평가 결과가 참인 경우 코드 블록을 실행. 그 후 조건식을 다시 평가하여 여전히 참인 경우 코드블록을 다시 실행하여 조건식이 거짓일 때까지 반복.

### for 문

```jsx
for (변수 선언문 또는 할당문; 조건식; 증감식){
	조건식이 참인 경우 반복 실행될 문;
}
```

- 반복 횟수가 명확할 때 주로 사용
- `for (;;) { ... }` 이렇게 어떤 식도 사용하지 않았을 때에는 무한루프가 된다.
- 중첩해 사용 가능

### while 문

```jsx
var count = 0;

while (count < 3) {
  console.log(count);
  count++;
} // --> 0, 1, 2 출력
```

- 반복 횟수가 불명확할 때 주로 사용한다.
- 조건식의 평가 결과가 언제나 참이면 무한루프가 된다.
  `while (true) { ... }`
- 무한루프에서 탈출하기 위해서는 코드 블록 내에 if문으로 탈출 조건을 만들고 break문으로 코드블록을 탈출
  ```jsx
  var count = 0;

  while (true) {
    console.log(count);
    count++;
    if (count === 3) break;
  } // --> 0, 1, 2 출력
  ```

### do~while 문

```jsx
var count = 0;

do {
  console.log(count); // 0 1 2
  count++;
} while (count < 3);
```

- 코드블록을 먼저 실행하고 조건식을 평가해 코드블록은 무조건 한 번 이상 실행됨

## break문

- 레이블 문, 반복문 또는 switch 문의 코드 블록을 탈출(이 코드 블록들 외에 사용 시 SyntaxError가 발생)
- 레이블 문: 식별자가 붙은 문으로 코드 실행 순서를 제어함(ex.`foo: - {console.log('foo')}` )

## continue문

- 반복문의 코드 블록 실행을 현 지점에서 중단하고 반복문의 증감식으로 실행 흐름을 이동시킴
